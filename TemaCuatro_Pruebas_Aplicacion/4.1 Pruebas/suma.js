// Función hipotética que será probada con Jest
// Recibe dos parámetros numéricos y retorna su suma
function suma(a,b){
    return a + b;
}

// Exportamos la función para poder usarla en el archivo de prueba
module.exports = suma;
